export const APP_HTTP_PORT = 8000;
export const APP_TLS_PORT = 8001;

export const isProduction = process.env.NODE_ENV === "production";
// export const isTesting = process.env.NODE_ENV === 'testing';
