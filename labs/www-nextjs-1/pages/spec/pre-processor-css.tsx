import { IEntity, Entity } from "../../mesh";

export const entity: IEntity = Entity.create("pre-processor-css");

export const Page = () => (
	<>
		<h1>CSS pre-processor</h1>
	</>
);

export default Page;
