import styles from "./alpha-alert.module.css";

export const AlphaAlert = () => (
    <div className={styles.alert}>
        We are under very early construction, so please indulge us.
    </div>
);

export default AlphaAlert;
