export const Page = () => (
    <>
        <h1>SDE.Team site</h1>

        <h2>Multi-language</h2>
        <p>
            We expect that content will be mostly code-generated that implies
            automatic generation of translations. So creating a separate page
            for each translation will cause a redundancy because should contain
            the same code as another translations do.
        </p>
    </>
);

export default Page;
