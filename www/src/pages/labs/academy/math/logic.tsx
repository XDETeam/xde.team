import React from "react";
import { Layout } from "../../../../templates/layout";

export const Logic = () => (
	<Layout>
		<header>
			<h1>Логика</h1>
		</header>

		<section>
			<h2>Логика высказываний</h2>
			<p>AKA исчисление высказываний, логика нулевого порядка</p>
		</section>

		<section>
			<h2>Логика первого порядка</h2>
			<p>AKA predicate logic, quantificational logic, first-order predicate calculus</p>
		</section>
	</Layout>
);

export default Logic;
