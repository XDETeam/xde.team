/**
 * Specified entity.
 * 
 * @remarks
 * Basically entity is just something we can point by our finger or give a name to it. Depends
 * on the context we can specify a lot of additional details and associate them with this entity,
 * but initially it's a term (name, string).
 * 
 */
export type SpecEntity = string;
